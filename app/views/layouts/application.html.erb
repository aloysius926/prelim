<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Prelims" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Prelims" %>">
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
     <%= render 'layouts/header' %>
    </header>
    <main role="main">
       <%= render 'layouts/messages' %>
       <div class="container">
         <%= yield %>
       </div>  
    </main>
   
  </body>
  <footer class="footer">
     <%= render 'layouts/footer' %>
  </footer>
</html>
