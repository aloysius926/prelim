= form_for(@questiontag, html: {class: "form-inline"})  do |f|
  .field
    = f.collection_select :tag_id, Tag.all, :id, :tag, {:class => 'input-small'}
  = f.hidden_field :question_id, :value => params[:id]
  = f.submit "Add Tag"